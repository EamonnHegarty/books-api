# books-api

This project is just a small api I have created to learn some devops &amp; gcp principles.

# Helper Command to test endpoint

curl -X POST http://localhost:3000/books/search -H "Content-Type: application/json" -d '{"query": "a scary horror book set in space"}'
